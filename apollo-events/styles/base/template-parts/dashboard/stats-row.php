<?php
/**
 * DEPRECATED — replaced by the form.html app-shell rebuild.
 * stats-row.php → stats.php ; charts-row.php → removed (mock charts dropped).
 * Inert stub (host mount does not permit file deletion).
 *
 * @package Apollo\Event
 */
if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
