<?php
/**
 * SUPERSEDED — replaced by styles-ds.php.
 *
 * This part inlined apollo-templates/assets/css/event-card.css, which is a
 * DIFFERENT .a-eve-card implementation from the one the mockup uses. That
 * substitution is why listed events rendered with no background. The real
 * source (ds-components.css, verbatim from the Design System handoff) is now
 * ported in styles-ds.php. Kept as a no-op so any stale include resolves.
 *
 * @package Apollo\Event
 * @deprecated 1.5.5 Use styles-ds.php.
 */
if ( ! defined( 'ABSPATH' ) ) { exit; }
