{
	"name": "apollo/apollo-calendar",
	"description": "Apollo Personal Calendar — per-user planner aggregating appointments, RSVPs, scheduler and holidays.",
	"type": "wordpress-plugin",
	"license": "proprietary",
	"require": {
		"php": ">=8.1"
	},
	"autoload": {
		"psr-4": {
			"Apollo\\Calendar\\": "src/"
		}
	},
	"minimum-stability": "stable",
	"prefer-stable": true
}
