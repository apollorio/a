# Hand unknown /evento/* paths to WordPress (CPT slug collides with this folder).
<IfModule mod_rewrite.c>
	RewriteEngine On
	RewriteBase /evento/
	RewriteCond %{REQUEST_FILENAME} !-f
	RewriteCond %{REQUEST_FILENAME} !-d
	RewriteRule . /index.php [L]
</IfModule>
